"""nth_dao.cli — operations-flavored entry points.

These commands are for humans running NTH DAO in production:

    python -m nth_dao.cli.status                 # snapshot of the local team
    python -m nth_dao.cli.metrics --port 9090    # serve Prometheus metrics
"""
